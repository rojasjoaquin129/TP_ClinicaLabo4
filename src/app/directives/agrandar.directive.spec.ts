import { AgrandarDirective } from './agrandar.directive';

describe('AgrandarDirective', () => {
  it('should create an instance', () => {
    const directive = new AgrandarDirective();
    expect(directive).toBeTruthy();
  });
});
