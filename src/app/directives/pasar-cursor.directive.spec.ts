import { PasarCursorDirective } from './pasar-cursor.directive';

describe('PasarCursorDirective', () => {
  it('should create an instance', () => {
    const directive = new PasarCursorDirective();
    expect(directive).toBeTruthy();
  });
});
